create a diagram that depicts data pipeline generation steps: 
1) the user provides natural language instructions to the LLM model. 
2) LLM model interpret the instructions and identify data engineering steps. 
3) LLM generates the code for each step and for the whole data pipeline